version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: cs_hub_db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: cs_hub_ai
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - cs_hub_network

  app:
    build: .
    container_name: cs_hub_app
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "postgresql://user:password@db:5432/cs_hub_ai?schema=public"
      NEXTAUTH_SECRET: "your-super-secret-nextauth-string"
      NEXTAUTH_URL: "http://localhost:3000"
      OPENAI_API_KEY: "your-openai-api-key"
    depends_on:
      - db
    networks:
      - cs_hub_network

networks:
  cs_hub_network:
    driver: bridge

volumes:
  postgres_data: